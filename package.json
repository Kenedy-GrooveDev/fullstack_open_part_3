{
  "name": "fullstack_open_part_3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \\\\\\\"Error: no test specified\\\\\\\" && exit 1",
    "build:ui": "rm -fr dist && cd ../fullstack_open_part_2/the_phonebook && npm run build && cp -r dist ../../fullstack_open_part_3",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Kenedy GrooveDev",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^9.0.1",
    "morgan": "^1.10.1"
  }
}
